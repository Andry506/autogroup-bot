meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt

run:
  containerPort: 8000
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000
